namespace Logictracker.MiniMT.v1
{
    public class AtRequest
    {
        /*public byte[] ToByteArray()
        {
            var buffer = new byte[4 + Comando.Length];
            buffer[0] = Convert.ToByte(Convert.ToInt16(apiNumber)/256);
            buffer[1] = Convert.ToByte(Convert.ToInt16(apiNumber));
            buffer[2] = Convert.ToByte(Convert.ToInt16(Tipo));
            buffer[3] = 0;
            Array.Copy(Encoding.ASCII.GetBytes(Comando),0,buffer,4,Comando.Length);
            return buffer;
        }*/
    }
}